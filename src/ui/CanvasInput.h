#ifndef SIRIUS_CANVAS_INPUT_H
#define SIRIUS_CANVAS_INPUT_H

class Editor;

void handleCanvasMouse(Editor& editor, bool canvasHovered);
void handleCanvasPanZoom(Editor& editor, bool canvasHovered);

#endif
