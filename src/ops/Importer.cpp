#include "ops/Importer.h"

#include "app/Project.h"

bool Importer::loadProject(Project& project, const std::string& path) {
    (void)project;
    (void)path;
    return false;
}

bool Importer::loadImage(Project& project, const std::string& path) {
    (void)project;
    (void)path;
    return false;
}
